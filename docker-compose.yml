version: '3.7'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
          NG_APP_API_URL: 'http://localhost:8081'
          NG_APP_APP_HOST: 'http://localhost:4200'
    ports:
      - '4200:80'
